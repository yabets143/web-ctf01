<%- include('partials/header', { title: 'Dashboard • Parameter Pollution CTF' }) %>

<div class="row g-4">
  <div class="col-12 col-lg-7">
    <div class="card">
      <div class="card-header"><strong>Profile</strong></div>
      <div class="card-body">
        <p class="mb-3">Signed in as <strong><%= profile.username %></strong></p>
        <form method="post" action="/update-profile" class="d-grid gap-3">
          <div>
            <label class="form-label">Username</label>
            <input class="form-control" name="username" value="<%= profile.username %>" />
          </div>
          <div>
            <label class="form-label">Bio</label>
            <textarea class="form-control" name="bio" rows="4"><%= profile.bio || '' %></textarea>
          </div>
          <button class="btn btn-primary" type="submit">Save</button>
        </form>
      </div>
    </div>
  </div>
  <div class="col-12 col-lg-5">
    <div class="card">
      <div class="card-header"><strong>Lab Demo Object</strong></div>
      <div class="card-body">
        <p class="text-secondary">Global object used by the vulnerable admin route.</p>
        <pre><%= JSON.stringify(labUser, null, 2) %></pre>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
